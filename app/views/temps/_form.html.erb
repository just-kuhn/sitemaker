<%= form_for(temp) do |f| %>
  <% if temp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(temp.errors.count, "error") %> prohibited this temp from being saved:</h2>

      <ul>
      <% temp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div style="margin-top: 5%; margin-left: 40%">
  <div class="field">
    <%= f.label :temp_image %>
    <%= f.text_field :temp_image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  </div>
<% end %>
